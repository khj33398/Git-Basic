/*
    index.html 에서 사용되는 js
*/

function login(username, password) {
    //login 처리
    if(username != null) {
        //username값과 FLAG 값을 체크
        //conflict test
        //conflict test2
    }
    return true;
}

function logout(username) {
    //logout 처리
    
}